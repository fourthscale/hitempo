/**
 * Sprint 16 — re-export under the legacy name. New code should import
 * `MailCredentialsServiceFactory` from `@/lib/mail/`.
 */
export { MailCredentialsServiceFactory as GmailCredentialsServiceFactory } from "@/lib/mail/mail-credentials-service-factory";
